# ⚠️ CONFIDENCIAL - NO COMMITEAR
# Renombar a secrets.yaml y llenar con valores reales

gcp:
  project_id: "your-gcp-project-id"
  credentials_path: "/path/to/credentials.json"

sftp:
  host: "sftp.retailcompany.com"
  port: 22
  username: "your-sftp-user"
  password: "your-sftp-password"
  remote_path: "/retail-claims"

gcs:
  bucket_name: "retail-claims-etl-prod"

email:
  alerts_to: "data-team@company.com"
  smtp_server: "smtp.gmail.com"
  smtp_port: 587
  sender: "etl-alerts@company.com"
